<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f3f4f6;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      scrollbar-width: none; /* Firefox */
      -ms-overflow-style: none; /* IE 10+ */
    }
    body::-webkit-scrollbar {
      display: none !important; /* Chrome, Safari, Opera */
    }

    .form-box {
      background-color: #ffffff;
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 400px;
    }

    .form-box h2 {
      text-align: center;
      margin-bottom: 20px;
      color: #333333;
    }

    .form-box input,
    .form-box button {
      width: 100%;
      padding: 10px;
      margin-bottom: 15px;
      border: 1px solid #cccccc;
      border-radius: 4px;
      font-size: 14px;
    }

    .form-box input:focus {
      outline: none;
      border-color: #999999;
    }

    .form-box button {
      background-color: #4b5563;
      color: #ffffff;
      border: none;
      cursor: pointer;
    }

    .form-box button:hover {
      background-color: #374151;
    }
  </style>
</head>
<body>

  <div class="form-box">
    <% if (typeof error !== 'undefined' && error) { %>
      <div id="error-toggle" style="background:#ffeaea; color:#d90429; border:1px solid #d90429; border-radius:6px; padding:10px 16px; margin-bottom:18px; text-align:center; font-weight:600; transition: opacity 0.5s;">
        <%= error %>
      </div>
      <script>
        setTimeout(function() {
          var err = document.getElementById('error-toggle');
          if (err) {
            err.style.opacity = '0';
            setTimeout(function() { err.style.display = 'none'; }, 500);
          }
        }, 2500);
      </script>
    <% } %>
    <h2>Login</h2>
    <form action="login" method="POST">
      <input name="email" type="email" placeholder="Email Address" required />
      <input name="password" type="password" placeholder="Password" required />
      <button type="submit">Log In</button>
      <p style="text-align: center; margin-top: 15px;">
        Don't have an account? <a href="/user/signup" style="color: #4b5563; text-decoration: none;">Sign Up</a>
    </form>
  </div>

</body>
</html>
